export class Dress { 
  constructor(
    public name: string,
    public link: [string, number], // Array type containing string and number
    public image: string,
    public price: string
  ) {} 
}
